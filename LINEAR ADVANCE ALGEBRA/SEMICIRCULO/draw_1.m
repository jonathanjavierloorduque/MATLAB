th = linspace( pi/2, -pi/2, 100);
R = 1/2;  %or whatever radius you want
x = R*cos(th) ;
y = R*sin(th) ;
plot(y,x); axis equal;

N=4;
for i=; N 
    R=1/2;
    theta= lindspace (pi/2, -pi/2, 100);
    x =R*cos(theta);
    y =R*sin(theta);
    plot(y,x);exis equal

    
end